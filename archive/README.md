# Archive

This folder contains legacy or reference-only assets that are no longer part of
the active runtime structure but may still be useful for future reference.

Current contents
- `archive/figma` — archived design system and reference components.
- `archive/client-legacy` — archived legacy frontend files and backups.

Use this folder for historical material only; active application code should live
in `client/` and `backend/`.
